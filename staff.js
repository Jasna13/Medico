// Function to show the form for adding a new user
function showAddUserForm() {
    document.getElementById('add-user-form').style.display = 'block';
}

// Function to hide the add user form
function hideAddUserForm() {
    document.getElementById('add-user-form').style.display = 'none';
}

// Function to update a user (placeholder, should be implemented)
function updateUser(username) {
    alert(`Update user: ${username}`);
    // Add logic to handle updating a user
}

// Function to delete a user (placeholder, should be implemented)
function deleteUser(username) {
    alert(`Delete user: ${username}`);
    // Add logic to handle deleting a user
}
